[pytest]
testpaths = tests
addopts = -ra
asyncio_mode = auto
# E2E tests (TUN, root) are opt-in via --e2e
markers =
    e2e: end-to-end tests requiring root/admin privileges (deselect with -m "not e2e")
